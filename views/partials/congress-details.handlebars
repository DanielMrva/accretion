{{#if loggedIn}}
<section>
    <div class="card">
        <div class="card-block">
            <h4 class="card-title">Congressional Hearing: {{congress.topic}}</h4>
            <h6 class="card-subtitle text-muted">Employee Attending: {{congress.emp_id}}</h6>
            <h6 class="card-subtitle text-muted">Representative: {{congress.reps_comm}}</h6>
            <h6 class="card-subtitle text-muted">Office: {{congress.off_id}}</h6>
        </div>
    </div>
</section>
{{else}}
<a href="/login">You must login in order to be able to search this criteria!</a>
{{/if}}